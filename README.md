# mini-zine-maker
This is a python project that will use streamlit as a frontend. The goal is to allow users to upload images to turn into a one paper mini zine that they can print, fold, cut into a mini zine.
